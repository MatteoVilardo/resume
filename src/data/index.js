import { PROFILE } from "./profile";
import { PROJECTS } from "./projects";
import { EXPERIENCE } from "./experience";
import { THEMES, DEFAULT_THEME_KEY } from "./themes";
import { STRINGS } from "./strings";
import { NAV_ITEMS } from "./navigation";
import { TECH_GROUPS } from "./tech";

export {
  PROFILE,
  PROJECTS,
  EXPERIENCE,
  THEMES,
  DEFAULT_THEME_KEY,
  STRINGS,
  NAV_ITEMS,
  TECH_GROUPS,
};

export const DATA = {
  profile: PROFILE,
  projects: PROJECTS,
  experience: EXPERIENCE,
  themes: THEMES,
  strings: STRINGS,
  navItems: NAV_ITEMS,
  techGroups: TECH_GROUPS,
};
